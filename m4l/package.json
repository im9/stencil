{
  "name": "@stencil/m4l",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Stencil M4L target — pnpm workspace root for engine and host packages.",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "bake": "node scripts/maxpat-to-amxd.mjs",
    "bake:check": "node scripts/maxpat-to-amxd.mjs --check"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
