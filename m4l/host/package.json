{
  "name": "@stencil/host",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "M4L host layer for Stencil. Bridges Max transport and the Turing engine: owns step state, resolves step→note, shapes MIDI events. Loaded by [node.script] in Stencil.amxd via stencil.mjs; unit-tested standalone under node --test.",
  "main": "./dist/host/host.js",
  "scripts": {
    "test": "node --test",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  }
}
