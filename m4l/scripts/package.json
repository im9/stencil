{
  "name": "@stencil/scripts",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Bake pipeline for Stencil M4L (.maxpat -> .amxd) and patcher guard tests. Per ADR 004.",
  "scripts": {
    "test": "node --test",
    "build": "echo 'no build for scripts package'",
    "typecheck": "echo 'no typecheck for scripts package (plain JS)'"
  }
}
